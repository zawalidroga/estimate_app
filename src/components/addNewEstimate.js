import React from 'react';

const NewEstimate = (props) => {
    

    return(
        <div>
            <button onClick = {props.addNew}> + </button>
        </div>
    )
}

export default NewEstimate; 